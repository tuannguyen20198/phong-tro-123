const initState = {
  isLoggedIn: false,
  token: false,
};
const authReducer = (state = initState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default authReducer;
